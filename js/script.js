// loader code 
let spinner = document.querySelector('.swiper-wrapper');

window.addEventListener('load', () => {
  
    // spinner.style.display = "none";

    setTimeout(() => {
        spinner.style.display = "none";
    },500);

});